apiVersion: v1
kind: Service
metadata:
  name: checkride-shuma-frontend
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app.kubernetes.io/instance: app-instance
    app.kubernetes.io/name: app
  sessionAffinity: None
  type: ClusterIP